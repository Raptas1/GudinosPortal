{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Backend",
            "request": "launch",
            "command": "npm start",
            "cwd": "${workspaceFolder}/portal-backend"
        }
    ]
}